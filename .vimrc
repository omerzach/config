
" Keep cursor centered vertically on screen
:set scrolloff=999

" Disable backups, swap files, and undo files
set nobackup
set nowritebackup
set noswapfile
set noundofile

" Configure search
set wrapscan
set hlsearch
set incsearch
set ignorecase
set smartcase
